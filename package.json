{
  "name": "clearcreator",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered design tool — Canva-like poster/flyer generator",
  "scripts": {
    "start:web": "cd apps/web && npm start",
    "start:api": "cd apps/api && npm run dev",
    "start": "concurrently \"npm run start:web\" \"npm run start:api\"",
    "build:web": "cd apps/web && npm run build",
    "build:api": "cd apps/api && npm run build",
    "build": "npm run build:web",
    "install:all": "cd apps/web && npm install && cd ../.. && cd apps/api && npm install && cd ../.."
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
